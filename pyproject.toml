[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "torch-nested-ad-narrow-guard"
version = "0.5.1"
description = "Guards six real torch correctness bugs: nested (second-order) forward-mode AD silently returning the wrong value for slogdet, householder_product, and layer_norm, torch.func.jacfwd chained three deep silently zeroing higher-order derivatives through a custom autograd.Function, torch.nested.narrow(..., layout=torch.jagged) + unbind silently including an unselected element, and a padded<->jagged nested-tensor roundtrip that crashes the backward pass"
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.9"
authors = [{ name = "zhuhroscar-tech" }]
classifiers = [
    "Programming Language :: Python :: 3",
    "Operating System :: POSIX :: Linux",
    "Operating System :: MacOS",
    "License :: OSI Approved :: MIT License",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
]
dependencies = []

[project.optional-dependencies]
dev = ["pytest", "pytest-cov"]
torch = ["torch>=2.0"]

[project.scripts]
torch-nested-ad-narrow-guard = "torch_nested_ad_narrow_guard.cli:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
